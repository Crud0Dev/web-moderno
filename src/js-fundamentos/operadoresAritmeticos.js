const [a, b, c, d] = [3, 5, 1, 15]

const soma = a + b + c + d // syntax chamada inFix op1 + op2 "entre os dois", preFix --d, 
const subtracao = d - b
const multiplicacao = a * b
const divisao = d / a
const modulo = a % 2

console.log(soma, subtracao, multiplicacao, divisao, modulo)

/**
 * Operadores: 
 * Unário = -operador
 * Binário = quando existem 2 operadores
 */